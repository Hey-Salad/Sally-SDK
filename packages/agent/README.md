# sally-agent

The Sally agent runs on the machine that has iOS or Android hardware attached. It will watch for devices, open tunnels, coordinate the stream server, and report heartbeats to the worker.

The package is scaffolded and ready for daemon implementation.

